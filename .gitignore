# Dependencies. The plugin has none — neither runtime nor development — so
# vendor/ should never appear; ignored so a stray `composer require` cannot
# commit one by accident.
/vendor/

*.log

# Release artefact.
/dist/
/kntnt-transparent-header-ollie-pro.zip

# Editor / OS files.
.DS_Store
.idea/
.vscode/
*.swp
*~
